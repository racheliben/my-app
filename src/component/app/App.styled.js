import { styled } from "@mui/material/styles";

export const StyledContainer = styled("div")({
  display: "flex",
  justifyContent: "center",
  padding: "70px 5%",
});

export const StyleUsersTable = styled("div")({
  width: "100%",
  position: "sticky",
  top: "70px",
  alignItems: "flex-start",
});

export const StyledWapperUsersTable = styled("div")({
  width: "100%",
});
